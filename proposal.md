# Woori Aroma 예약 시스템 — 프로젝트 기획안

> One Time. One Team.
> 제주 중문 프라이빗 스파 "우리같이아로마"를 위한 다국어 예약 및 운영 플랫폼

작성일: 2026-08-10

---

## 1. 프로젝트 개요

### 1.1 배경

우리같이아로마는 현재 Calendly(`calendly.com/wooriaroma`)로 예약을 받고 있다. Calendly는 범용 스케줄링 도구라 다음과 같은 한계가 있다.

- 매장의 실제 운영 규칙(그룹 단위 예약, 준비/정리 버퍼 시간, 인원별 가격 등)을 반영할 수 없다.
- 예약금(디포짓) 정책을 자동으로 계산·수납할 수 없다.
- 다국어(영/한/중/일) 대응이 매장 상황에 맞게 커스터마이징되지 않는다.
- 예약 데이터가 매장 운영 데이터(방문 이력, 유입 경로, 매출)로 축적되지 않는다.

이 프로젝트는 Calendly를 매장 전용 예약 시스템으로 대체하는 것이 목표다. Calendly의 "단순함"은 유지하되, 우리같이아로마의 실제 운영 방식에 맞춘 예약 경험을 새로 설계한다.

### 1.2 매장 정보

| 항목 | 내용 |
|---|---|
| 상호 | 우리같이아로마 (Woori Aroma) |
| 위치 | 제주 서귀포시 중문관광로 285 (중문) |
| 업태 | 프라이빗 스파 (그룹 전체 대여) |
| 최대 인원 | 4명 |
| 핵심 컨셉 | One Time. One Team. |
| 현재 예약 도구 | Calendly |
| 주요 해외 유입 경로 | Google Maps / Google 검색 |

### 1.3 제품 구성

| 인터페이스 | 주 사용자 | 언어 |
|---|---|---|
| 고객 예약 웹사이트 | 제주를 방문하는 해외 관광객 | 다국어 (영어 기본) |
| 관리자 대시보드 | 한국인 매장 직원 | 한국어 전용 |

고객 예약 사이트는 Google Maps, Google 검색, Instagram, 호텔 추천, 직접 링크 등으로 유입되는 트래픽이 대부분 모바일이므로 **모바일 퍼스트**로 설계한다.

---

## 2. 목표

Google Maps 방문자를 최소한의 마찰로 확정 예약까지 전환시키는 예약 경험을 만든다.

```text
Google Maps / 검색 / Instagram
              ↓
        우리같이아로마 웹사이트
              ↓
          인원 선택
              ↓
          시술 선택
              ↓
        날짜·시간 선택
              ↓
         고객 정보 입력
              ↓
          예약 확인
              ↓
         예약금 결제
              ↓
          예약 확정
              ↓
        관리자 대시보드
```

장기적으로는 Calendly, 메신저, 수기 관리로 흩어져 있는 예약 운영을 이 플랫폼 하나로 통합한다.

---

## 3. 고객 경험 (Customer Booking Site)

### 3.1 언어 처리

- 기본 언어: **영어**
- 지원 언어: 영어 / 한국어 / 中文 / 日本語
- 최초 방문 시 브라우저 로케일을 감지해 자동으로 언어를 선택한다.
- 사용자가 언어를 수동으로 변경하면 자동 감지보다 우선하며, 이후 방문에도 유지된다.
- **언어를 바꿔도 예약 진행 상태(선택한 인원/시술/날짜 등)는 초기화되지 않는다.**
- 구현: `next-intl` + 로케일 기반 라우팅(`/en`, `/ko`, `/zh`, `/ja`) + 쿠키/localStorage로 선호 언어 저장

### 3.2 예약 플로우

1. **랜딩** — "PRIVATE SPA · JEJU", "One Time. One Team.", "Up to 4 Guests" 메시지와 "Book Your Experience" CTA. 긴 소개 페이지가 아니라 전환 중심의 짧은 랜딩.
2. **인원 선택** — 1~4명, 그룹 단위로 1건의 예약이 생성된다.
3. **시술 선택**
   - 아로마 오일: 60분 / 90분(추천) / 120분 — 1인당 ₩100,000 / ₩140,000 / ₩180,000
   - 핫스톤: 90분 / 120분 / 150분 — 1인당 ₩160,000 / ₩200,000 / ₩250,000
   - 페이셜: 가격 미확정, 추후 확장 가능한 구조로만 준비
   - 시술 정보는 `data/services.ts`에 중앙화하여 컴포넌트 코드 수정 없이 추가/변경 가능
4. **날짜·시간 선택** — 실시간 예약 가능 시간 계산
5. **고객 정보 입력** — 이름, 전화/WhatsApp, 이메일, 국가, 선호 언어, 특이사항 (국제 전화번호 형식 지원)
6. **예약 검토**
7. **예약금 결제** (현재는 mock 결제)
8. **예약 확정** — 예약번호(`WA-YYYYMMDD-NNN`), 위치 보기(Google Maps), 캘린더 추가, 문의하기(WhatsApp) 액션 제공

### 3.3 요금 정책

- 시술 총액 = 1인당 가격 × 인원수
- 예약금 = ₩10,000 × 인원수 (잔액은 매장에서 현장 결제)

---

## 4. 예약 규칙 (핵심 비즈니스 로직)

1. **동시간대 1팀 원칙** — 무관한 두 그룹이 같은 시간대를 동시에 예약할 수 없다. 1인 예약도 4인 예약과 동일하게 해당 시간 슬롯 전체를 점유한다.
2. **준비/정리 버퍼** — 모든 예약은 시술 전 60분(준비), 시술 후 60분(정리)이 자동으로 함께 차단된다.
   - 예: 90분 아로마 오일 16:00~17:30 예약 → 실제 차단 구간은 15:00~18:30
3. **서버가 가용성의 최종 기준** — 클라이언트에서 계산한 예약 가능 시간은 참고용이며, 예약 확정 직전 서버가 다시 한 번 검증한다(더블부킹 방지).

핵심 로직은 UI와 분리하여 `lib/booking/`에 함수 단위로 둔다: `calculateBlockedTime()`, `checkBookingConflict()`, `generateAvailableSlots()` 등.

---

## 5. 관리자 대시보드 (Admin Dashboard)

한국인 직원 전용, **한국어 고정**(브라우저 로케일에 따라 자동 전환되지 않음). 라우트: `/admin`

| 영역 | 기능 |
|---|---|
| 오늘 현황 | 오늘 예약 팀 수, 방문 인원, 예상 매출, 받은 예약금 |
| 예약 관리 | 예약 목록, 일/주/월 캘린더, 상세, 추가/변경/취소, No-show 처리 |
| 시간 관리 | 특정 시간 차단, 휴무일 설정, 영업시간 설정 |
| 고객 관리 | 고객 정보, 방문 횟수, 최근 방문, 선호 언어, 예약 이력, 특이사항 |
| 서비스 관리 | 프로그램 활성/비활성, 가격/소요시간 변경, 예약금 정책 |
| 유입 경로 | GOOGLE_MAPS, GOOGLE_SEARCH, INSTAGRAM, NAVER, HOTEL, DIRECT, REPEAT 소스 추적 |

---

## 6. 기술 스택 및 아키텍처

### 6.1 프론트엔드 (구현됨)

- Next.js (App Router) + TypeScript
- Tailwind CSS
- `next-intl` (다국어 라우팅/번역)
- React 19

### 6.2 백엔드 / 데이터베이스 (미정 — 확인 필요)

`docs`(README2.md) 상의 원안은 **Cloudflare Workers + Cloudflare D1**이나, 현재 코드의 mock 데이터 계층 주석은 **Supabase**를 향후 대체 대상으로 언급하고 있어 두 문서 간 방향이 어긋나 있다. Phase 2 착수 전에 다음을 확정해야 한다.

- 호스팅/함수: Cloudflare Workers vs. Next.js API Routes(Vercel 등) vs. Supabase Edge Functions
- DB: Cloudflare D1(SQLite) vs. Supabase(Postgres)

어느 쪽을 선택하든 `lib/booking/`의 함수 시그니처(`BlockedWindow[]` 등)를 그대로 유지하도록 설계되어 있어 교체 비용은 낮다.

### 6.3 향후 연동

결제 게이트웨이, Google Maps, Google Calendar, WhatsApp, SMS, 카카오, 이메일 알림, 애널리틱스

### 6.4 데이터 모델 개념

```text
customers          — 예약자 정보
services            — 시술/옵션/가격
reservations        — 예약 (인원, 시작/종료, 차단 구간, 금액, 상태, 유입경로, 로케일)
reservation_guests   — 인원별 시술 선택(향후 확장용)
blocked_times        — 수동 차단, 휴무, 정비 등
```

향후 확장: `payments`, `notifications`, `admin_users`, `audit_logs`

### 6.5 API 개념

브라우저가 DB에 직접 쓰지 않고 반드시 API를 경유한다.

```http
GET  /api/availability?date=2026-08-12
POST /api/reservations
GET  /api/reservations/:reservationNumber
```

더블부킹 방지 절차:

```text
1. 가능한 시간 노출
2. 고객이 시간 선택
3. 예약 제출
4. 서버가 가용성 재검증
5. 여전히 가능 → 예약 생성
6. 이미 마감 → 거절 + 최신 가능 시간 재제공
```

---

## 7. 현재 구현 상태 (2026-08-10 기준)

Phase 1(고객 예약 UI)이 mock 데이터/mock 결제/mock 알림 기반으로 대부분 구현되어 있다.

**구현됨**
- `app/[locale]/book` — 다국어 라우팅 및 예약 위저드 (`BookingWizard`, `BookingProvider`, `BookingProgress`)
- 예약 단계별 컴포넌트: 인원(`GuestsStep`) → 시술(`TreatmentStep`) → 소요시간(`DurationStep`) → 날짜(`DateStep`) → 시간(`TimeStep`) → 고객정보(`DetailsStep`) → 검토(`ReviewStep`) → 결제(`PaymentStep`) → 확정(`ConfirmationStep`)
- 가용성/가격 로직: `lib/booking/availability.ts`, `pricing.ts`, `calendar.ts`, `time.ts`, `timezone.ts` (Asia/Seoul 기준 처리 포함)
- 서비스 정의 중앙화: `data/services.ts`
- 다국어 메시지: `messages/{en,ko,zh,ja}.json`
- Mock 결제(`lib/payment/mockProvider.ts`), Mock 알림(`lib/notifications/mockNotificationService.ts`) — 실제 연동 없이 인터페이스만 구현되어 향후 실서비스 교체를 대비

**미구현**
- 실제 백엔드 API / 데이터베이스 연동 (현재 `lib/booking/mockData.ts`의 메모리 데이터로 동작)
- 관리자 대시보드 — `app/admin/page.tsx`는 12줄짜리 placeholder만 존재
- 실 결제, 실 알림(WhatsApp/이메일), Google Maps/Calendar 연동
- 인증/보안, 유입 경로 트래킹, 애널리틱스

---

## 8. MVP 단계별 로드맵

### Phase 1 — 고객 예약 UI (진행 중, 대부분 완료)
랜딩, 언어 감지/전환, 인원·시술·기간·날짜·시간 선택, 고객 정보, 예약 검토, mock 결제, 확정 화면

### Phase 2 — 백엔드 & 데이터베이스 (착수 전)
호스팅/DB 스택 확정 → 스키마 마이그레이션 → 가용성 API → 예약 API → 충돌 검증 → 예약번호 생성 → 에러 처리

### Phase 3 — 관리자 대시보드 (착수 전)
한국어 관리자 UI, 관리자 인증, 오늘 현황, 캘린더, 예약 상세/수기 등록/변경/취소, 시간 차단, 고객 목록, 서비스 관리

### Phase 4 — 결제 & 알림 (착수 전)
실 예약금 결제, 결제 검증, 확정 이메일, WhatsApp 알림, 관리자 알림, 취소 플로우

### Phase 5 — 성장 (착수 전)
Google Maps 예약 링크, 유입 경로 트래킹, 애널리틱스, 리뷰 요청 자동화, 재방문 고객 CRM, AI 예약 어시스턴트

---

## 9. 개발 원칙

1. 모바일 퍼스트
2. 언어를 전환해도 예약 진행 상태를 잃지 않는다
3. 매장 데이터(가격, 시술 등)를 컴포넌트에 하드코딩하지 않는다
4. 비즈니스 로직은 UI 컴포넌트 밖에 둔다
5. 예약 가능 여부의 최종 기준은 서버다
6. 더블부킹을 방지한다
7. "1건의 예약 = 1개의 프라이빗 그룹"을 기준으로 설계한다
8. 관리자가 DB를 직접 건드리지 않고도 운영할 수 있게 한다
9. DB 스키마 변경은 마이그레이션으로 관리한다
10. MVP는 단순하게, 그러나 깔끔한 확장 지점을 남겨둔다

---

## 10. UI/브랜드 방향

- 컨셉: 제주 프라이빗 스파 + 부티크 호텔 + 미니멀 웰니스 브랜드
- 팔레트: 웜 아이보리, 샌드, 베이지, 차콜, 뮤트 올리브
- 지양: 범용 SaaS 스타일, 과도한 그라디언트/네온/그림자/애니메이션
- `docs/images/`의 실제 매장 사진(외관, 트리트먼트룸, 족욕 공간, 리셉션)을 1차 비주얼 레퍼런스로 사용하며, 실제 공간과 다른 이미지를 임의로 생성하지 않는다

---

## 11. 열린 질문 (Phase 2 착수 전 결정 필요)

- 백엔드/DB: Cloudflare(Workers + D1) vs. Supabase — 코드 주석과 문서 원안이 불일치하므로 확정 필요
- 결제 게이트웨이 선정 (해외 카드 결제 지원 여부 포함)
- 관리자 인증 방식
- WhatsApp/이메일 알림 발송 주체(자체 구현 vs. 서드파티 서비스)
