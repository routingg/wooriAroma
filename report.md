# Woori Aroma 예약 시스템 개발 진행 보고서

최종 업데이트: 2026-08-15 (Gemini AI 예약 상담 기능 구현, 보안 검토, 관리자 인증 추가, `https://wooriaroma.site` 실배포까지 반영)

## 1. 프로젝트 개요

본 프로젝트는 제주 중문에 위치한 프라이빗 스파 **Woori Aroma(우리같이아로마)**의 기존 예약 방식을 개선하고, 고객 예약부터 관리자 운영까지 하나의 시스템에서 처리할 수 있는 전용 예약 플랫폼을 구축하는 것을 목적으로 한다.

기존 Calendly 중심의 예약 방식에서 벗어나 Woori Aroma의 실제 운영 방식인 **“One Time. One Team.”** 원칙을 반영하고, 해외 관광객을 고려한 다국어 예약 환경과 향후 AI 기반 자연어 예약 상담 기능까지 확장할 수 있는 구조로 개발을 진행하였다. 기획상 시스템은 고객 예약 웹사이트, 관리자 대시보드, AI 예약 에이전트의 세 영역으로 구성된다.

---

## 2. 개발 목표

이번 개발에서 중점적으로 고려한 사항은 다음과 같다.

* 모바일 환경에서 간편하게 예약할 수 있는 고객 예약 페이지 구축
* 영어·한국어·중국어·일본어 다국어 지원
* Woori Aroma의 실제 서비스 및 가격 정보 제공
* 한 시간대 한 팀만 예약할 수 있는 프라이빗 예약 구조 적용
* 예약 가능 시간과 기존 예약 간 충돌 방지
* 관리자 중심의 예약 확인 및 확정 프로세스 구축
* 예약 및 고객 데이터를 자체적으로 축적할 수 있는 구조 마련
* 이메일 알림 및 관리자용 메일 작성 기능 구축
* 향후 Gemini 기반 AI 예약 상담 기능과 연결 가능한 API 구조 설계

---

# 3. 주요 구현 내용

## 3.1 고객용 다국어 예약 시스템

고객이 모바일 또는 PC에서 직접 예약 신청을 진행할 수 있는 Booking Wizard를 구현하였다.

현재 예약 화면은 다음과 같은 단계로 구성된다.

**인원 선택 → 트리트먼트 선택 → 이용시간 선택 → 날짜 선택 → 시간 선택 → 고객정보 입력 → 예약 내용 확인 → 예약 신청 완료**

예약 페이지는 `next-intl`을 기반으로 영어, 한국어, 중국어, 일본어를 지원하며 각 언어별 URL 구조를 사용한다. 현재 프로젝트에는 고객 예약 위저드와 다국어 메시지 파일이 모두 구성되어 있다.

특히 해외 관광객의 사용성을 고려하여 영어를 기본 언어로 제공하면서 사용자가 원하는 언어를 선택할 수 있도록 구성하였다.

---

## 3.2 트리트먼트 및 가격 정보 구조화

마사지 프로그램 정보를 개별 화면에 하드코딩하지 않고 `data/services.ts`에 중앙화하였다.

이를 통해 시술명, 이용시간, 가격 등의 데이터를 한 곳에서 관리할 수 있으며 향후 관리자 페이지에서 프로그램을 수정할 수 있는 구조로 확장할 수 있도록 설계하였다.

현재 Woori Aroma의 핵심 프로그램인 아로마 오일 마사지와 핫스톤 마사지 등을 중심으로 예약할 수 있도록 구성되어 있으며 향후 Thai Massage, Foot Massage, Facial Spa 등 전체 실제 매장 메뉴와 연동할 수 있는 방향으로 개발하고 있다.

---

## 3.3 예약 가능 시간 계산 로직

예약 화면에 표시되는 시간을 단순한 고정 시간표가 아닌 예약 상황과 운영 규칙을 바탕으로 계산할 수 있도록 별도의 Booking Domain Logic을 구성하였다.

현재 프로젝트에는 다음과 같은 예약 관련 모듈이 존재한다.

* Availability 계산
* 가격 계산
* 날짜 및 시간 처리
* Asia/Seoul 시간대 처리
* 예약 요청 validation

이를 UI와 분리하여 `lib/booking/` 영역에서 관리하고 있어 향후 고객 예약 페이지, 관리자 페이지, AI 예약 에이전트가 동일한 예약 규칙을 사용할 수 있도록 하였다.

기획 단계에서 설정한 핵심 원칙인 **동시간대 한 팀 예약**과 예약 충돌 방지 구조 역시 이 Booking Domain을 중심으로 처리하도록 설계되어 있다.

---

# 4. 예약 운영 정책 개선

개발을 진행하면서 초기 기획안의 예약 방식에서 실제 매장 운영에 맞게 중요한 정책 변경을 진행하였다.

초기 기획에서는 고객이 예약금을 결제하면 예약이 바로 확정되는 구조를 고려했지만, 실제 운영에서는 관리자가 최종 스케줄을 확인할 필요가 있다고 판단하였다.

이에 현재 시스템은 다음 방식으로 변경되었다.

**고객 예약 신청 → PENDING → 관리자 확인 → CONFIRMED → 고객에게 확정 안내**

또한 온라인 예약금 결제를 제거하고 **매장 방문 후 현장 결제 방식**으로 변경하였다.

이러한 방식은 자동 예약으로 인한 운영 충돌을 방지하면서도 고객이 온라인에서 편리하게 예약 요청을 할 수 있도록 하기 위한 구조이다.

---

# 5. 관리자 예약 관리 기능

관리자가 고객 예약을 확인하고 실제 운영 상태에 맞게 관리할 수 있는 관리자 페이지를 구축하였다.

관리자 화면은 고객용 화면과 달리 **한국어 전용**으로 구성되어 있으며 고객 예약정보와 예약 상태를 관리하는 데 초점을 맞추고 있다.

현재 관리자 예약 관리 기능을 통해 예약 접수 내용을 확인하고 예약 상태를 관리할 수 있다.

예약 상태는 운영 흐름에 따라 다음과 같이 관리할 수 있도록 구성되어 있다.

* PENDING — 예약 확인 대기
* CONFIRMED — 예약 확정
* COMPLETED — 방문 완료
* CANCELLED — 예약 취소
* NO_SHOW — 노쇼

---

# 6. 완료 예약 Soft Delete 기능

관리자의 예약 목록이 장기간 누적될 경우 운영 화면이 복잡해지는 문제를 해결하기 위해 예약 삭제 기능도 추가하였다.

단, 고객 예약 데이터를 실제 데이터베이스에서 완전히 제거하지 않고 `deleted_at` 값을 기록하는 **Soft Delete 방식**을 사용하였다.

따라서 COMPLETED, CANCELLED, NO_SHOW 상태의 예약은 관리자 운영 목록에서 삭제할 수 있지만 실제 예약 기록은 데이터베이스에 남아 추후 고객 이력이나 통계에 활용할 수 있다.

---

# 7. 이메일 알림 시스템

예약 과정에서 필요한 고객 알림을 지원하기 위해 이메일 시스템을 구축하였다.

현재 자동 발송 대상으로 설계된 이메일은 다음과 같다.

* 고객 예약 신청 접수 안내
* 예약 취소 안내
* 방문 24시간 전 리마인더

각 이메일 발송 시도는 `notifications` 테이블에 기록되며, 성공·실패·skip 상태를 관리할 수 있도록 구성하였다. 동일한 리마인드 이메일이 반복 발송되는 문제를 방지하기 위한 idempotency 구조도 적용되어 있다.

메일 발송 서비스는 Resend와 연결할 수 있도록 구성하였다.

---

# 8. 예약 확정 메일 수동 작성 및 복사 기능

Woori Aroma의 실제 운영에서는 모든 예약 확정 메일을 자동으로 발송하기보다 관리자가 내용을 직접 확인한 뒤 고객에게 보내는 것이 적합하다고 판단하였다.

이에 따라 예약이 CONFIRMED 상태가 된 후 관리자 페이지에서 고객에게 전달할 확정 메일을 생성할 수 있도록 개발하였다.

메일은 두 가지 형태를 제공한다.

1. **Plain Text 버전**
2. **디자인이 적용된 HTML 버전**

관리자는 고객 이름, 이메일 및 예약 내용을 확인한 뒤 메일 내용을 필요에 따라 수정하고 복사하여 Gmail 등의 이메일 서비스에서 직접 발송할 수 있다.

따라서 온라인 예약에 대한 `RESERVATION_CONFIRMED` 이메일은 시스템이 자동 발송하지 않는다.

메일을 실제 발송한 후에는 예약 상태와 별도로 **메일 발송 완료 여부**를 관리할 수 있도록 구성하였다.

---

# 9. 데이터베이스 및 Repository 구조

초기 개발 단계에서는 Mock Data를, 이후에는 Node.js 내장 `node:sqlite` 모듈 기반 로컬 SQLite를 사용하였다. 이후 실제 배포 환경(Cloudflare Workers)에 맞추어 데이터베이스를 **Cloudflare D1**으로 전환하였다. 로컬 개발 시에는 `wrangler d1 migrations apply woori-aroma-db --local` 명령으로 로컬 D1 인스턴스에 동일한 스키마를 적용하여 사용한다.

데이터 접근 로직은 UI나 예약 로직에서 직접 SQL을 실행하는 구조가 아니라 `lib/repositories/`를 통해 분리하였다.

현재 주요 구조는 다음과 같다.

* Booking domain
* Database
* Repository
* Notification
* Admin logic
* Agent Tool Layer

이 구조를 통해 향후 데이터베이스가 변경되더라도 상위 예약 로직에 미치는 영향을 최소화하도록 설계하였다.

---

# 10. API 기반 구조

고객 화면과 관리자 화면이 데이터베이스를 직접 수정하지 않고 서버 API와 Repository를 거쳐 처리하도록 구조화하였다.

프로젝트에는 예약 관련 API Route Handler가 구성되어 있으며 향후 AI Agent 역시 동일한 Booking API 및 Domain Logic을 사용할 수 있는 구조로 개발하였다.

이를 통해 다음과 같은 원칙을 유지할 수 있다.

**고객 UI / 관리자 UI / AI Agent → 동일한 예약 규칙 → 동일한 데이터베이스**

이는 예약 시간이나 가격 정보가 서로 다른 화면에서 다르게 계산되는 문제를 방지하기 위한 구조이다.

---

# 11. 24시간 전 예약 리마인더

고객 방문을 놓치거나 예약 시간을 잊는 것을 줄이기 위해 예약일 24시간 전 이메일을 발송하는 Reminder 구조도 추가하였다.

`/api/cron/reminders/` endpoint를 별도로 구성하였으며 외부 scheduler에서 주기적으로 호출할 수 있도록 설계하였다. Reminder는 중복 발송 방지를 고려하여 notifications 기록과 연결된다.

---

# 12. 테스트 환경 구축

예약 시스템의 안정성을 높이기 위해 Vitest 기반 테스트 환경도 구성하였다.

현재 테스트 대상에는 다음 영역이 포함된다.

* Booking Domain Logic
* API Route
* 관리자 기능
* Agent Tool
* Availability
* Pricing
* Validation

프로젝트에서는 `npm test`, `npm run typecheck`, `npm run build` 등을 통해 기능 및 타입 오류를 확인할 수 있도록 구성되어 있다.

---

# 13. Gemini 기반 AI 예약 상담 Agent 구현

초기 기획에서부터 고객이 자연어로 예약 문의를 할 수 있는 **AI 예약 에이전트**를 장기적인 핵심 기능으로 설정하였으며, 이번 개발 단계에서 **Google Gemini(`@google/genai` 공식 SDK)와의 실제 연동을 완료**하였다.

핵심 설계 원칙은 "AI가 기존 예약 시스템을 대체하지 않는다"는 것이다. Gemini는 데이터베이스에 직접 접근하지 않으며, 서버가 화이트리스트로 관리하는 Tool만 호출할 수 있고, 모든 Tool은 기존 Booking Domain Logic·Repository를 그대로 재사용한다.

```
고객 채팅 UI
   ↓
POST /api/agent/chat
   ↓
Gemini (Function Calling)
   ↓
서버 Tool Dispatcher (lib/agent/toolRunner.ts) — 화이트리스트 밖 호출은 전부 거부
   ↓
기존 Booking Domain / Repository / Database
```

구현된 Tool은 다음과 같다.

* `getServices` — 실제 서비스/가격 카탈로그 조회
* `getAvailability` — 실제 예약 가능 시간 조회 (기존 충돌 검증 로직 재사용)
* `calculatePrice` — 실제 가격 계산 (Gemini가 임의로 계산하지 않음)
* `createReservationRequest` — 예약 요청 생성 (**항상 PENDING**, 예치금/즉시확정 없음)
* `getReservationStatus` — 예약 조회 (이메일·전화번호로 본인 확인된 경우만)
* `handoffToAdmin` — AI가 처리할 수 없는 상황을 관리자에게 이관

`confirmReservation`, 예약 삭제, 할인/환불 등 관리자 전용 기능은 애초에 Gemini가 호출 가능한 목록에 존재하지 않도록 설계하여, 잘못된 프롬프트나 응답으로도 접근이 불가능하다.

---

# 14. 홈페이지 AI 상담 UI 구현

Google Gemini를 실제 홈페이지에서 사용할 수 있도록 AI 상담 UI 개발을 완료하였다.

홈페이지 Hero 영역에 기존 **Book Your Experience**(Primary CTA) 아래 **“AI와 예약상담하기”**(Secondary CTA)를 5종 테마 전체에 배치하였으며, AI 버튼이 시각적으로 기존 예약 버튼보다 강조되지 않도록 절제된 스타일(밑줄 텍스트)로 구성하였다.

버튼을 클릭하면 브라우저 네이티브 `<dialog>` 기반의 모달(데스크톱)/바텀시트(모바일)가 열리며, 포커스 트랩·ESC 닫기·`aria-modal` 등 접근성 요소는 네이티브 엘리먼트를 활용해 별도 구현 없이 충족하였다.

상담창을 처음 열면 빈 화면 대신 Quick Question(FAQ) 버튼을 제공한다.

* 오늘 예약 가능한가요?
* 2명 예약하고 싶어요.
* 어떤 마사지를 추천하나요?
* 마사지 가격이 궁금해요.
* 매장 위치가 어디인가요?
* 마사지 시간은 얼마나 걸리나요?

버튼 클릭 시 화면에 보이는 짧은 문구와 실제 Gemini에게 전달되는 문장을 분리하여 관리하며(예: "마사지 가격이 궁금해요" → "트리트먼트 가격을 알려주세요."), "매장 위치" 질문만은 AI 호출 없이 기존 Google Maps 링크·주소 데이터를 즉시 보여주어 응답 지연과 정보 오류 가능성을 없앴다.

AI 상담 UI는 현재 사이트 언어(영어·한국어·중국어·일본어)를 그대로 이어받아 Gemini에게 전달하며, 4개 언어 전체에 대해 FAQ 문구와 상담창 UI 텍스트 번역을 완료하였다. Gemini API 장애 시에도 "AI 상담을 잠시 이용할 수 없습니다 — 기존 예약 폼을 이용해주세요" 메시지와 함께 기존 Booking Wizard로 안내하여, AI 기능 장애가 전체 예약 시스템 장애로 이어지지 않도록 하였다.

실제 배포 전, 로컬 개발 서버와 브라우저 자동화 도구(Playwright)를 이용해 데스크톱/모바일 뷰포트, 4개 언어, 실제 Gemini 응답까지 포함한 end-to-end 검증을 진행하였고, 이 과정에서 발견된 문제 2건(클라이언트에 번역 메시지가 전달되지 않던 next-intl 설정 문제, AI 응답에 Markdown 문법이 그대로 노출되던 문제)을 실사용 중에 직접 확인하고 수정하였다.

---

# 15. 보안 검토 및 관리자 인증 추가

AI 기능을 포함한 전체 변경 사항에 대해 별도의 보안 검토를 진행하였다.

**AI Agent 관련 검토 결과**: Gemini가 호출 가능한 Tool은 화이트리스트로 제한되어 있고, Tool 인자는 Gemini의 응답을 그대로 신뢰하지 않고 서버에서 재검증하며, 예약은 항상 PENDING으로만 생성되고, API Key는 클라이언트나 로그에 노출되지 않는 것을 확인하였다. 신규로 도입된 코드에서 High/Medium 등급의 보안 취약점은 발견되지 않았다.

**검토 과정에서 발견한 기존 문제**: `/admin` 관리자 대시보드에 인증이 전혀 없어, URL만 알면 누구나 전체 고객 예약 정보(이름·이메일·전화번호)를 열람하거나 예약을 취소·삭제할 수 있는 상태였다. 이는 이번 AI 기능과 무관하게 프로젝트 초기부터 존재하던 문제였으나, 실제 배포를 앞두고 최우선으로 해결하였다.

조치 내용:

* `middleware.ts`에 `/admin` 전용 HTTP Basic Auth 게이트를 추가
* 인증 정보(`ADMIN_BASIC_AUTH_USER`/`ADMIN_BASIC_AUTH_PASSWORD`)가 설정되지 않은 경우 모든 요청을 거부하는 **Fail-Closed** 방식으로 구현 (기존 리마인더 크론의 `CRON_SECRET` 검증과 동일한 원칙)
* 실제 배포 환경(Cloudflare)에 해당 인증 정보를 Secret으로 등록하고, 프로덕션에서 인증 없이 접근 시 401, 올바른 인증 시 정상 접근되는 것을 직접 확인

---

# 16. main 병합 및 실제 배포

모든 기능 개발과 보안 조치를 마친 뒤 `develop` 브랜치를 `main`으로 병합하고 실제 서비스에 배포하였다.

* 배포 대상: Cloudflare Workers (`@opennextjs/cloudflare` + Wrangler)
* 실제 서비스 주소: **https://wooriaroma.site** (www.wooriaroma.site)
* 배포 전 `npm test`(134개 테스트 통과), `npm run typecheck`, `npm run lint`, `npm run build`를 모두 재확인
* 배포 과정에서 로컬 설정 파일(`wrangler.jsonc`)에 커스텀 도메인 라우팅 정보가 누락되어 있던 것을 발견하여, 실제 원격 설정과 동기화하고 재배포함으로써 향후 배포 시 설정 유실 위험을 제거

---

# 17. 초기 기획 대비 발전한 부분

프로젝트를 실제 운영 상황에 맞추는 과정에서 초기 기획보다 발전하거나 추가된 기능도 존재한다.

대표적으로 다음과 같다.

### ① 예약 승인 방식 현실화

초기 예약금 기반 자동확정 방식 대신 **PENDING → 관리자 확인 → CONFIRMED** 구조를 도입하였다.

### ② 확정 이메일 수동 발송

관리자가 고객별 상황에 맞게 이메일 내용을 확인하고 직접 발송할 수 있도록 Plain Text와 HTML 두 가지 메일 버전을 제공하였다.

### ③ Soft Delete

운영 화면에서는 완료 예약을 제거하면서도 고객과 예약 이력은 보존하도록 개선하였다.

### ④ 이메일 발송 기록

예약 상태와 이메일 발송 상태를 분리하여 실제 운영 여부를 보다 정확하게 관리할 수 있도록 하였다.

### ⑤ 예약 리마인더

방문 24시간 전 자동 Reminder 기능을 추가하였다.

### ⑥ AI 예약 상담 실제 연동 및 배포

기획 단계의 단순 AI Agent 개념에서 더 발전하여 Google Gemini Function Calling을 실제로 연동하고, 홈페이지 AI 상담 버튼·Quick Question·자연어 예약 상담·PENDING 예약 생성까지 전 과정을 구현하여 실서비스에 배포하였다.

### ⑦ 관리자 인증 추가

배포를 앞두고 발견한 `/admin` 무인증 문제를 HTTP Basic Auth(Fail-Closed 방식)로 해결하고 실제 배포 환경에 적용하였다.

---

# 18. 현재 미완성 및 향후 개발 과제

현재 고객 예약, 기본 관리자 운영, AI 예약 상담 기능까지 구축·배포되었으나 실서비스 정식 운영을 위해 추가 개발이 필요한 기능도 존재한다.

우선순위가 높은 과제는 다음과 같다.

### 관리자 인증 고도화

현재는 아이디/비밀번호 한 쌍을 공유하는 Basic Auth 방식이다. 직원별 계정 구분, 세션 관리, 감사 로그(audit log)를 갖춘 정식 로그인 시스템으로 발전시킬 필요가 있다.

### 예약 수정 및 일정 변경

현재 `RESERVATION_UPDATED` 이벤트 및 템플릿 구조는 존재하지만 실제 예약 수정 및 reschedule 기능과 연결되어 있지 않다.

### 관리자 운영 기능 확대

향후 다음 기능을 추가할 필요가 있다.

* 예약 캘린더
* 영업시간 관리
* 휴무일 관리
* 특정 시간 차단
* 서비스 및 가격 관리
* 고객 방문 이력 관리

### 유입경로 및 Analytics

Google Maps, Google Search, Instagram, Naver, Hotel, Repeat 등 고객 유입경로를 저장하고 예약 전환율을 분석할 수 있는 기능도 향후 구축할 예정이다. 기획안에는 해당 유입 경로 관리 기능이 관리자 대시보드 요구사항으로 정의되어 있다.

---

# 19. 향후 개발 방향

AI 예약 상담(Gemini Function Calling) 연동과 배포, 관리자 인증 추가는 완료되었다. 향후 개발은 다음 순서로 진행하는 것이 적절하다.

**1단계 — 관리자 기능 완성**
예약 수정/reschedule, 시간 차단·휴무일·영업시간 UI, 서비스 및 가격 관리, 직원별 관리자 계정

**2단계 — 데이터 활용**
고객 유입경로 추적, 예약 통계, 매출 및 고객 분석

**3단계 — 운영 자동화**
리뷰 요청, 재방문 고객 관리, CRM 및 마케팅 자동화, AI 상담 범위 확장(예약 변경/취소 등)

---

# 20. 결론

Woori Aroma 예약 시스템은 단순한 예약 페이지 개발에서 시작하여 현재는 **고객 예약, 예약 상태 관리, 데이터베이스, 이메일 알림, 관리자 운영, Gemini 기반 AI 예약 상담까지 연결된 통합 예약 플랫폼**으로 발전하였고, `https://wooriaroma.site`에 실제 배포되었다.

특히 실제 매장 운영에 맞게 자동확정이 아닌 관리자 승인 방식으로 예약 프로세스를 변경하고, 예약 확정 메일을 관리자가 직접 검토하여 발송하도록 구성하면서 실제 현장에서 사용할 수 있는 방향으로 시스템을 개선하였다. AI 예약 상담 역시 같은 원칙을 따라 — 기존 Booking API/Domain Logic을 그대로 재사용하고, 항상 PENDING 상태로만 예약을 생성하며, 관리자 승인 이전에는 어떤 경우에도 "확정되었다"고 안내하지 않도록 구현하였다.

배포를 앞두고는 별도의 보안 검토를 진행하여 AI 기능 자체의 신규 취약점이 없음을 확인하는 한편, 그동안 미뤄져 있던 관리자 인증 부재 문제를 발견하고 배포 전 해결하였다.

고객은 이제 기존 Booking Wizard와 AI 상담 중 자신에게 편한 방식을 선택하여 예약할 수 있으며, 최종적으로는 예약 접수만 처리하는 시스템을 넘어 **고객 유입 → 예약 상담 → 예약 관리 → 고객 커뮤니케이션 → 재방문 관리까지 연결하는 Woori Aroma 전용 운영 플랫폼**으로 발전시키는 것을 목표로 한다.
